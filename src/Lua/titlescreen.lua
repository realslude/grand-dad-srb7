-- reusing stuff from srb5b yay
/* addHook("HUD", function(v)
	v.drawScaled(FU, 15*FU, FU, v.cachePatch("ONENINENINETWO"), V_SNAPTOBOTTOM)
end, "copyright")

addHook("HUD", function(v)
	v.drawScaled(FU, FU, 4*FU, v.cachePatch("GRANDDAD"))
end, "title")
*/
-- nvm doesnt work